import XCTest
@testable import CountryCodesList

final class CountryCodesListTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(CountryCodesList().text, "Hello, World!")
    }
}
